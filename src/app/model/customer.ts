import {st} from '@angular/core/src/render3';

export class Customer {
  customerId: number;
  companyName: string;
  firstName: string;
  lastName: string;
  type: string;
  occupation: string;
  address: string;
  phone: string;
  email: string;
  status: string;
  createdDate: Date;
  selected: boolean;
}
